Code Review Env - Submission Fix Summary
Date: 2026-04-08

Final Status
- Local validator passes: ping, Docker build, and openenv validate.
- Repository is clean (no uncommitted changes).
- Latest fixes are already synced to origin/main, hf/main, and hf2/main.
- Current HEAD: 96d13799

Problems Encountered and Fixes Applied

1) openenv validate failed (multi-mode deployment readiness)
Issues:
- Missing uv.lock
- Missing server entry point in pyproject
- server/app.py missing callable main() and __main__ guard
- pyproject parser compatibility issues in remote checker

Fixes:
- Added server entry point wiring in pyproject.
- Added callable main() and __name__ == '__main__' execution path in server/app.py.
- Generated and committed uv.lock.
- Adjusted pyproject format to be checker-friendly.
- Synced stale Hugging Face remotes (hf and hf2) to latest main.

2) inference.py unhandled exception (server URL unreachable)
Issue:
- Script raised ConnectionError when default URL http://localhost:8000 was not reachable.

Fixes:
- Improved startup URL resolution with fallback candidates and env vars.
- Changed default URL preference to localhost:7860.
- Added graceful error handling and controlled exit behavior.
- Kept fallback behavior when HF token is missing.

3) Output parsing failed (no structured blocks)
Issue:
- Validator could not parse JSON-based logs for required [START]/[STEP]/[END] blocks.

Fixes:
- Replaced JSON-only structured logs with plain tagged stdout lines:
  [START] ...
  [STEP] ...
  [END] ...
- Added flush=True to ensure immediate stdout delivery.

4) Task score validation failed (scores out of range)
Issue:
- Validator requires each task score strictly inside (0,1), disallowing 0.0 and 1.0.

Fixes:
- Added strict score normalization in server/environment.py:
  - MIN_TASK_SCORE = 0.01
  - MAX_TASK_SCORE = 0.99
  - Applied across grade_task1/2/3 and invalid-task paths.
- Hardened inference structured output to avoid emitting out-of-range score fields.
- Removed score-like fields in logs that could be interpreted as invalid task scores.
- Ensured emitted score values remain within (0,1).

Key Commits (latest first)
- 96d13799 Make structured score fields strictly within (0,1) for validator
- 7811d86f Enforce strict (0,1) task scores for validator compatibility
- 3ad61f07 Emit parser-friendly [START]/[STEP]/[END] stdout blocks
- 696a7344 Make inference robust to env URL/port differences and avoid unhandled startup errors
- 0c12af2c Fix OpenEnv validation: add server entrypoint and lockfile

Operational Notes
- If the submission portal still shows old errors, wait briefly for remote rebuild/cache refresh and retry Update Submission.
- Ensure the checker is reading the latest commit on the same branch/remote.
